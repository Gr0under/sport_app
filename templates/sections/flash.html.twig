{% for message in app.flashes('success') %}
	<div class="flashMessage">{{message}}</div>
{% endfor %}

{% for message in app.flashes('error') %}
	<div class="flashMessage flashMessage--error">{{message}}</div>
{% endfor %}
